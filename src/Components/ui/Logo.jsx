const Logo = ({ className }) => {
    return (
        <div className={`flex flex-row justify-center items-center text-3xl font-bold ${className}`}>
            ⌚ xTask
        </div>
    );
};

export { Logo }; 